apiVersion: v1
kind: Pod
metadata:
  name: demo-pod
  namespace: training
  labels: 
    env: development
    project: training
    ownername: Tope
spec: 
  containers:
    - image: nginx
      name: ngix-server
      ports:
        - protocol: TCP
          containerPort: 80
          
